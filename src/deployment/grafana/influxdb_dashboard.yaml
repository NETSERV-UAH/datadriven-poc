apiVersion: 1
providers:
 - name: Default
   folder: 'IoT Sensors'
   type: file
   allowUiUpdates: true
   options:
    path: /etc/grafana/provisioning/dashboards
      #/etc/grafana/provisioning/dashboards
